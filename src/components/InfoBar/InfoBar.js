import React from "react";
import "./InfoBar.scss";

/**
 * The InfoBar component provides the user with information
 * about the image they are manipulating such as its size,
 * format, and more.
 */
const InfoBar = (props) => {

  return (
    <div className="infobar-wrapper">
      <div className="infobar">

      </div>
    </div>
  );

}

export default InfoBar;